import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.app',
  appName: 'demo-nx-cross-ddd-plugin',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
